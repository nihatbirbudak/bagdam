# Bağdam — Sistem Durumu

> Canlı/lokal sistemin anlık durumu. Her faz sonunda güncellenir. Son güncelleme: 2026-08-21 (F10 sonu)

## Faz durumu

| Faz | Durum | Not |
|---|---|---|
| F0 Karar sprinti | ✅ | 16 ADR (docs/adr), state-machines.md; kullanıcı aksiyonları (iyzico/e-posta sağlayıcısı/ETBİS) bekliyor |
| F1 Walking skeleton | ✅ (lokal) | Monorepo + apps/api + apps/admin + packages/shared + deploy/ dosyaları. Sunucu kurulumu ADR-0017 ile F10b'ye taşındı |
| F2 Şema-a + seed + pricing | ✅ | `database/schema.prisma` (23 model, F2a), 3 migration, seed idempotent, shared pricing 117 test (ADR-0018 kuralları dahil), health `db: up` |
| F3 Inline bootstrap + katalog dinamik | ✅ | `CatalogModule` + `/api/v1/bootstrap`; `partials/bootstrap.hbs`; cart.js 3 yama; products.js kaldırıldı; HTML diff 8/10 byte-byte + 2 kabul edilen satır; **Playwright 30/30 çift 0 px**; duman 14/14; API 70 test |
| F4 Admin iskeleti + auth + katalog CRUD + medya | ✅ (lokal) | `AuthModule` (cookie access 15 dk + refresh 30 gün rotasyon, CSRF double-submit, 5 hata → 30 dk kilit, roller) + guard zinciri Throttler → JwtAuth → Csrf → Roles; `AuditLogInterceptor` (redaksiyon) + `GET /admin/audit-logs`; `CatalogAdminController` (/api/v1/admin/products·lots·images·categories·producers·tiers·box-templates·box-week); `MediaModule` (multer 20 MB → sharp webp+thumb, `/uploads/*` statik) + `media:import` (85 görsel, idempotent); admin ekranları 1–8. API jest 6 suite/107 test; admin vitest 5/32; **Playwright e2e 12/12** (`tools/e2e-admin/report.md`) |
| F5 CMS içerik + günlük + yasal + toptan + ayarlar | ✅ (lokal) | `ContentModule` (SiteContent 22 anahtar × registry şeması + PUT doğrulama, Post, LegalDocument sürümlü publish/409/nav, Consent, `/sitemap.xml` + `/robots.txt` kökte) · `SettingsModule` (7 grup registry; sırlar AES-256-GCM `enc:v1:`, GET maskeli) · `DeliveryModule` (public zones/dates, admin zone CRUD + dates/generate) · `WholesaleModule` (POST 3/dk/IP + admin); WebController → `ContentSourceAdapter` (ContentService + CatalogService.listActiveCategories; Prisma okuyucusu kalktı); 10 .hbs'de `{{{site.*}}}` + 3 partial; içerik seed'i (22 · 11 · 3, idempotent); admin ekranları 9–15 (14a). API jest 14 suite/173 test; admin vitest 12 dosya/66 test; **Playwright parite 30/30 0 px + duman 14/14** (ContentService ile); **Playwright e2e F5 13/13** (`tools/e2e-admin/report-f5.md`) |
| F6 Üyelik + hesap + adres + e-posta çekirdeği | ✅ (lokal) | `AuthController` F6 uçları (register + KVKK/pazarlama Consent satırları tek işlemde + anında giriş · verify JWT 24 s → `?dogrulandi=1` · forgot sessiz 200 + sha256 token 60 dk · reset tek kullanımlık, diğer oturumlar düşer) · `MeModule` (`/me/address` upsert tek adres, `/me/consents`, F8 yer tutucu orders/cards) · `MailModule` (SiteContent `mail.*` 6 Handlebars şablonu + seed → MailLog → SMTP (Setting mail.* → .env SMTP_*) / `DISABLE_MAIL` önizleme `logs/mail/<id>.html`; `Notifier` arayüzü + MailNotifier (auth + wholesale); `GET /admin/mail-logs`, `POST /admin/settings/mail/test`) · `CustomersModule` (ekran 16: liste/detay/PATCH/anonimleştir) · WebController bootstrap `me` çerezden (`private, no-store`) · cart.js `BahcedenCart.api()` (CSRF, 401 → refresh/çıkış, Türkçe hata haritası) + uyelik/sepet auth kapıları, adres formu (ilçe select), `?sifirla`/`?dogrulandi` dalları, toptan formu api() (ADR-0003 istisna 2/4/5) · admin ekran 16 + Sistem › E-posta günlüğü + Ayarlar › E-posta test. API jest 18 suite/195 test; admin vitest 18 dosya/87 test; **Playwright parite 30/30 0 px (mask `#forgotNote`) + duman 14/14**; **e2e F6 19/19** (`tools/e2e-admin/report-f6.md`) |
| F7 Şema-b + fiyatlama + abonelik motoru | ✅ (lokal) | Şema F2b (`0003_commerce`/`0004_raw_commerce`: Cart/Order/OrderLine/PaymentMethod/Payment/Refund/WebhookEvent/Subscription/SubscriptionCycle/CycleItem/SubscriptionEvent/SubscriptionCancellation + minimal Coupon; 37 model) · `PricingModule` (quote/cycleCharge — shared computeQuote + Setting commerce.* + DeliveryZone kargo + kullanıcı bağlamı) · `PaymentsModule` (PaymentProvider arayüzü + ManualProvider + PaymentProviderFactory; ChargeStrategy MIT/PAYMENT_LINK; PaymentsService Payment/Refund/WebhookEvent; `GET /pay/:linkToken` JSON) · `DeliveryDatesService` (atomik rezerv/iade, findOrCreateFor/nextFor/isLocked) · `OrdersModule` (createFromQuote/createForCycle/createDeltaForCycle/transition; `/me/orders`, `/orders/:no/cancel`, `/admin/orders` + CSV) · `SubscriptionsModule` (cycles:ensure/lock-and-charge/expire-payment-links, dunning 08:00 sınırı, skip/unskip, swap/pref/extras/merge-cart, PATCH freq/gün/adres/kart, tek seferlik → COMPLETED, iptal akışı + retention, telafi, SubscriptionEvent; `/me/subscription*`, `/admin/subscriptions|cycles|ops/*`; **`SUBSCRIPTIONS_DEPS` = `SubscriptionsDepsAdapter` → gerçek Pricing/Payments/Orders/Delivery**; `POST /admin/subscriptions` manuel checkout — F8 öncesi açılış yolu) · `JobsModule` (6 @Cron yalnız scheduler instance + CronLog + `POST /admin/jobs/:name/run {now?}` geliştirme/test). API jest **28 suite/259 test** (TZ=UTC/Europe/Istanbul/America/New_York'ta aynı); shared vitest 117; **e2e F7 11/11** `tools/e2e-admin/run-f7.mjs` (API düzeyi simülasyon: 3 müşteri, 5 haftalık saat ilerletme, MIT/LINK/tek seferlik, dunning → PAST_DUE → toparlama, iptal/retention, pick/packing, CronLog; temizlik ≡ başlangıç) — rapor `report-f7.md` |
| F8 Checkout + sipariş + PayTR + kupon | ✅ (lokal) | **PayTR (A):** `modules/payments/providers/paytr/*` — saf hash yardımcıları (iFrame get-token · bildirim · Link API · durum sorgu · iade · kayıtlı kart; HMAC-SHA256/base64, `toMerchantOid`, `buildUserBasket`, zamanlama-güvenli `verifyCallbackHash`), enjekte edilebilir `PAYTR_HTTP` (fetch; testlerde mock — canlı/test API'sine istek ATILMADI), `PayTrConfigService` (Setting `payment.*` → `.env PAYTR_*`), `PayTrProvider` (initCheckout iFrame token · chargeStoredCard · refund · createPaymentLink · verifyWebhook; `storedCardEnabled` kapalıyken 503 `PROVIDER_FEATURE_DISABLED`), `POST /api/v1/payments/paytr/callback` (@Public @SkipCsrf @SkipThrottle; IP allowlist → hash → WebhookEvent idempotent → `PaymentsService.settlePayment` → Order PAID/PAYMENT_FAILED + PaymentMethod + Notifier; düz metin "OK"), `PaymentProviderFactory` `paytr` kaydı · **Checkout (B):** `modules/checkout` (`POST /checkout/quote` @Public + `POST /checkout` tek `$transaction`: onaylar → kupon → DD rezerv → Subscription PENDING + cycle#1 → Order + satır snapshot → Payment PENDING (`ord<no>`+4) → sağlayıcı init → Consent satırları), `GET /orders/:no/status`, `modules/coupons` (validate + PricingService uygulaması + CouponRedemption + admin CRUD), `payments:reconcile` (@Cron 15 dk; 30 dk PENDING → retrieve / 24 s → EXPIRED + Order CANCELLED + DD iade), `mail.order-paid` (SiteContent registry + seed), `/me/cards` (+ `add-session` 501), `POST /admin/payments/:id/refund` · **Web (C):** `views/sepet.hbs` checkout API'ye bağlandı (özet `POST /checkout/quote`'tan — istemci hesaplamaz; `GET /delivery/dates` gün seçimi; `GET /legal` requiresAck onay kutuları; kupon alanı; PayTR iFrame konteyneri; `GET /orders/:no/status` 2 s/120 s polling; `?siparis=&odeme=ok|hata` dönüş dalı), `kutu.hbs` "aboneliği başlat" → sepet, `uyelik.hbs` siparişler/kartlar/abonelik API'den; `bahceden_card`/`bahceden_orders` localStorage KALDIRILDI · **Admin (D):** ekran 17 Siparişler (liste+filtre+CSV, detay: satırlar/ödemeler/iade/durum geçişleri/notlar/fatura), Kuponlar ekranı, Ayarlar › Ödeme PayTR alanları + test modu uyarısı, Özet bugünkü sipariş/ciro kartı, Müşteri detayı siparişler. API jest **33 suite/301 test**; admin vitest **23 dosya/120 test**; shared vitest 117; **Playwright parite 30/30 0 px (mask `#forgotNote`) + duman 14/14**; **e2e F8 17/17** `tools/e2e-admin/run-f8.mjs` (rapor `report-f8.md`) |
| F9 Web etkileşimli sayfalar + ops ekranları | ✅ (lokal) | **API (C):** ops uçları (`GET /admin/ops/pick-list` · `packing-list` · `day-summary`, `POST /admin/ops/bulk-status` — hep-ya-hiç, `skipInvalid` ile kısmi), `GET /admin/payment-issues` (UNPAID/AWAITING_PAYMENT cycle + PAYMENT_FAILED sipariş tek listede + `counts`), `GET /admin/dashboard` (ekran 21), bootstrap `serverNow` + WebController'da gömülü `__BAGDAM__.sub`, SiteContent registry **31 anahtar** (`uyelik.texts` 49 alan · `sepet.texts` 29 alan; seed idempotent) · **Web (A):** `cart.js` `BahcedenCart.remote` (tüm abonelik mutasyonları API'de; `applyServerSub` tek doğruluk kaynağı, optimistic güncelleme yok) + `serverNow()`/`CLOCK_SKEW_MS` + `nextCutoff/isDeliveryDayLocked/lockedDeliveryDay` → `__BAGDAM__.deliveryDates`; `kutu.hbs` canlı modda tier/tür kilitli (ADR-0008) + taslak → `liveApplySteps`; `uyelik.hbs` durum şeritleri · atla/geri al · iptal + kalma teklifi akışı · `sepet.hbs` `#addToBoxBtn` → merge-cart; `bahceden_card/orders/retention_offered/address/member/session` localStorage'dan **kaldırıldı** (kalan: `bahceden_cart`, `bahceden_prefs`, satın alınmamış `bahceden_sub` taslağı) · **Admin (B):** ekran 14b Teslimat tarihleri · 18 Ödeme Problemleri · 19 Abonelikler (liste + detay: cycle geçmişi, olay günlüğü, iptal kayıtları, telafi) · 20 Teslimat Günü (toplama/paketleme + yazdırma + toplu durum) · 21 Özet. API jest **37 suite/320 test**; admin vitest **34 dosya/210 test**; shared vitest 117; **Playwright parite 30/30 0 px (mask `#forgotNote`) + duman 14/14**; **e2e F9 19/19** `tools/e2e-admin/run-f9.mjs` (rapor `report-f9.md`) |
| F10 Bildirimler + çerez/KVKK + sertleştirme + şema dondurma | ✅ (lokal) | **A — bildirim + çerez + KVKK:** `MailTemplateSlug` 7 → **16** (ADR-0014 listesi tam: `cycle-charged` · `cycle-payment-failed` · `cycle-awaiting-payment` · `cutoff-reminder` · `order-shipped` · `order-delivered` · `order-delivery-failed` · `subscription-cancelled` · `subscription-past-due`), her biri SiteContent registry + seed (Türkçe, `{{brand.*}}`); `SubscriptionNotifier` artık `MailNotifier`'a bağlı (F7 stub'ı kapandı) — `emit()` senkron ve fırlatmaz, gönderim **transaction dışında** 250 ms gecikmeyle (`NOTIFIER_MAIL_DELAY_MS`), `emitAndDeliver()` + `whenIdle()`; `OrdersService.transition` tek huniden OUT_FOR_DELIVERY/DELIVERED/DELIVERY_FAILED → `order.shipped|delivered|delivery-failed`; `reminders:cutoff` gerçek gönderime bağlandı (cycle başına bir kez — MailLog `(templateSlug, entityId)` tekilliği). Çerez şeridi `views/partials/cookie-consent.hbs` (Zorunlu/Analitik/Pazarlama; Reddet · Yönet · Kabul Et) **sunucuda gizli** basılır, `cart.js` `// F10 cookie:` bloğu gösterir → **ADR-0003 istisna 11**; karar `localStorage bagdam_cookie_consent` + `POST /consents`. KVKK: Setting grubu `privacy` (6 alan), `kvkk:purge` job (MailLog+önizleme / SystemLog / CronLog yaş bazlı silme — eski `logs:cleanup` içinde — + AuditLog PII `[silindi]` + opsiyonel pasif hesap anonimleştirme), `docs/kvkk-veri-saklama.md` (18 satırlık matris) + KVKK metnine "Saklama süreleri" paragrafı. · **B — güvenlik + ekran 22:** yola göre üç CSP (`common/security/content-security-policy.ts`; web/admin/api), HSTS yalnız production, `X-Powered-By` kapalı, `X-Frame-Options: DENY`; hata zarfına `code` alanı (F8 açık notu kapandı); PII redaksiyonu genişletildi; `env-validator` production zorunluları; `__tests__/security/**` (yetki matrisi · IDOR · CSRF · kütle atama · SQL enjeksiyon · XSS · oturum kilidi · sır sızıntısı · dosya yükleme · hız sınırı); admin **ekran 22 Sistem** (Sağlık · Denetim · Sistem · Cron · E-posta · Webhook + işleri çalıştır) + `modules/{system-logs,cron-logs,webhook-events}` ve `GET /admin/health/detailed`. · **C — performans + yedek + runbook:** admin router `React.lazy` (1 → 86 chunk; ilk açılış 735,6 → ~348 kB; 500 kB uyarısı yok), `tools/load/run.mjs` (+ `report.md`) ve `tools/load/n1-scan.mjs` (Prisma log ile N+1 taraması), `tools/backup-restore-drill.mjs` (pg_dump -Fc → `bagdam_restore_test` → pg_restore → 16 tablo sayım karşılaştırması → DB düşür; `tools/backup-restore-report.md`), `docs/RUNBOOK.md`. · **D — entegrasyon:** CMS zengin metin **sunucuda temizleniyor** (`common/security/html-sanitize.ts`, yazma + render; temiz içerik byte-byte aynı → parite korunur), `privacy` grubu admin Ayarlar › Genel'e eklendi, `tools/hbs-check.mjs` (19/19), `docs/guvenlik-denetimi.md`, **ADR-0021 şema v1 donduruldu**. Doğrulama: API jest **47 suite / 395 test** · admin vitest **36 dosya / 228 test** · shared 117 · **parite 30/30 0 px** (`--mask=#forgotNote,#cookieConsent`) + duman 14/14 · **e2e F10 16/16** (`tools/e2e-admin/report-f10.md`, temizlik 17 tablo ≡ başlangıç) · seed idempotent · `pnpm db:status` güncel |
| F10b–F11 | ⬜ | Sunucu kurulumu + yayın (F10b), lansman + hypercare (F11) |

## Lokal ortam (doğrulandı 2026-08-21, F10 sonu)

| Veritabanı | ✅ PostgreSQL 18.4 `bagdam_dev` / `bagdam_test` (rol `bagdam`, citext, TZ Europe/Istanbul); `pnpm db:status` güncel — **şema F7 1. gün (2026-08-20): 37 model / 29 enum / 5 migration** (F8 şema değişikliği YOK — Coupon/CouponRedemption `0003_commerce` ile gelmişti) (`0003_commerce`: F2b 12 model + minimal Coupon/CouponRedemption + F2a karşı ilişkiler + `consents.orderId` FK; `0004_raw_commerce`: `"orders_orderNo_seq"` RESTART 1001 + `payments_provider_pid_succeeded`), 38 tablo (+`_prisma_migrations`), 78 timestamptz / 0 timestamp, shared enum paritesi 29/29 (`docs/erd.md`); seed: categories 4 · producers 15 · products 22 · product_images 27 · product_lots 22 · media_files 85 (seed 29 + `media:import` 56: ikonlar/logo/sahne/urunler) · box_tiers 2 · box_templates 2 (16 item) · delivery_zones 2 · delivery_dates 50 (seed ufku 8 hafta: 2026-08-22 → 2026-10-15; +2 satır 2026-08-21 seed koşusunda) · settings 37 (seed 31: commerce 17 — ADR-0018 üç kural dahil; seo/cookies/payment + panel/e2e kaynaklı 6 grup alanı) · site_content **31** (22 + 7 `mail.*` şablonu + **F9: `uyelik.texts` 49 alan · `sepet.texts` 29 alan**) · legal_documents 11 (nav 8 + hash 3, v1) · posts 3 · users 1 (admin) · mail_logs 0 · addresses 0 (F6 e2e/test verisi temizlenir); **F9 şema değişikliği YOK** (37 model / 5 migration aynı) |
|---|---|

| Bileşen | Durum |
|---|---|
| `pnpm install` | ✅ pnpm 9.15.9, 822 paket |
| `@bagdam/shared` | ✅ tsc + build + 117 vitest (TZ=UTC ve Europe/Istanbul) |
| `@bagdam/api` | ✅ tsc + nest build; `node dist/main.js` → http://127.0.0.1:4010 ; `GET /api/v1/health` 200; F5: `/sitemap.xml` (text/xml, 10 sayfa + günlük yazıları) ve `/robots.txt` kökte 200 (global prefix dışı); jest **47 suite/395 test** (gerçek DB, seri; F6: customer-auth 7 · me 4 · customers 4 · mail 7; F7: pricing 9 · payments 9 · delivery-dates 8 · orders service+http · subscriptions engine 9 / http 3 / capacity-template 2 / simulation 1 / manual-checkout 3; F8: checkout.http 10 · coupons 6 · paytr-hash · paytr-provider · paytr-callback.http; **F9: ops.http (pick/packing/day-summary/bulk-status + payment-issues) · dashboard.http · content/site-content-f9-keys 8 (registry ↔ seed ↔ uyelik.hbs/sepet.hbs, DB'siz)** — **F10: security/access-matrix · csrf-injection-xss · richtext-sanitize (13, D) · headers-and-envelope · secrets-and-upload · env-validator · zz-rate-limit · cookie/cookie-consent · jobs/kvkk-purge · mail/f10-notifier** — suite'ler kendi verisini (tier/bölge/kullanıcı/cycle/kupon/ödeme/CronLog/SystemLog) siler; `payment.*` ayar satırlarını koşu süresince değiştirip geri koyar) |
| e2e (F7, API simülasyonu) | ✅ `tools/e2e-admin/run-f7.mjs` — geçici API :4064 (`ENABLE_CRON=false`, `DISABLE_MAIL=true`; Playwright yok: fetch + psql); zaman job'lara verilen `now` ile ilerletilir (`POST /admin/jobs/:name/run {now}` — yalnız geliştirme/test): ayarlar dunning [2,12] + paymentLinkHours 1 (sonda geri) → 11 haftalık şablon → 3 müşteri kaydı (register + adres + psql saklı kart) → admin `POST /admin/subscriptions` ×3 (MIT haftalık Salı · PAYMENT_LINK kartsız · tek seferlik Perşembe; Order PAID MANUAL, cycle#1, ACTIVE; ikinci → 409; /me/subscription + /me/orders) → cycles:ensure (8 cycle, idempotent) · delivery-dates:generate · reminders:cutoff → T0 kesimi (cycle#1 CHARGED 0 TL; /admin/cycles; pick 4 satır / packing 2 fiş; ops PREPARING→OUT→DELIVERED) → tek seferlik R0 → COMPLETED, /me/subscription null → atla/geri al (DD −1/+1) → T1 MIT CHARGED + Order PAID + Payment CYCLE_CHARGE → PAYMENT_LINK: AWAITING + `GET /pay/:token` → expire → UNPAID (link EXPIRED) → retry yeni link → admin send-payment-link → 08:00 sınırı SKIPPED(UNPAID) → iptal (teklif → kabul → teklifsiz → onay → CANCELLED) → kart `fail:` → T2/T3 UNPAID (+2 s/+12 s) → SKIPPED(UNPAID) ×2 → PAST_DUE → kart düzelt → T4 CHARGED → ACTIVE → müşteri 1 iptal → CANCELLED (DD iade) → CronLog 20 koşu SUCCESS → **temizlik: sayımlar ≡ başlangıç, delivery_dates.reserved geri, ayarlar geri**; **11/11 OK**, rapor `tools/e2e-admin/report-f7.md` |
| Sayfa paritesi | ✅ 10/10 sayfa eski statik siteyle byte-byte aynı (`cmp`); `/` = index |
| Önbellek başlıkları | ✅ anonim `public, max-age=0, s-maxage=10`; `access_token` çerezli `private, no-store` |
| 404 | ✅ web → 404.hbs (text/html), `/api/*` → JSON zarfı; `/api/v1/index.html` 404 |
| SITE_MODE | ✅ `coming-soon`: `/` ve `/index.html` → coming-soon, diğer 9 sayfa 404, statikler 200 |
| Görsel parite (F3) | ✅ `tools/visual-parity/run.mjs` — eski 8080 vs yeni 4010, 10 sayfa × 390/820/1440, pixelmatch 0.1 → 30/30 OK (0 px), sepet/kutu duman 14/14; rapor `tools/visual-parity/report.md` |
| Görsel parite (F5, CMS) | ✅ aynı araç, eski 8080 vs geçici 4043 (ContentService kaynaklı `{{{site.*}}}`, `{{#each legal|legalDocs|posts|categories}}`): **30/30 0 px**, duman 14/14 — e2e öncesi ve geri alma sonrası iki kez. Byte düzeyinde yalnız: footer `mapsUrl` `&`→`&amp;`, nasil-seciyoruz metin içi `"`→`&quot;` (2 satır), gunluk prototipteki CRLF satır sonları (gövde metni aynı), politikalar 3 gizli taslak makale, toptan form script'i, bootstrap satırı |
| Görsel parite (F6, auth) | ✅ aynı araç, eski 8080 vs geçici 4053, `--mask=#forgotNote` (ADR-0003 istisna 4 bloğu iki tarafta da akıştan çıkarılır): **30/30 0 px**, duman 14/14 — e2e F6 ve temizlik sonrası (DB ≡ seed). Maskesiz ham fark yalnız istisna 4: uyelik 368 px (3 viewport), sepet 368 px (mobile/tablet), sepet desktop 13 581 px (bağlantı satırı giriş kutusunu uzatıp altındaki içeriği kaydırır — 956→1005 px); istisna 2 (KVKK/pazarlama kutucukları) gizli "üye ol" formunda, istisna 5 (ilçe select) yalnız oturumlu hesap kartında → anonim ekran görüntüsüne girmez; anonim 8 sayfa maskesiz de 0 px |
| Görsel parite (F8, checkout) | ✅ aynı araç, eski 8080 vs geçici 4074, `--mask=#forgotNote`: **30/30 0 px**, duman 14/14 (e2e F8 öncesi ve `sepet.hbs` düzeltmesi sonrası iki kez). Maskesiz ham fark yalnız ADR-0003 istisna 4 (F6 ile aynı: uyelik 368 px × 3 viewport, sepet 368 px mobil/tablet, sepet desktop 13 581 px) — **F8 istisna blokları (kupon alanı, yasal onay kutuları, PayTR iFrame konteyneri, saklı kart listesi, "yeni kart ekle") yalnız OTURUMLU görünümlerde çizilir → anonim ekran görüntüsüne girmez**; `kutu.hbs` markup'ı hiç değişmedi (yalnız JS) |
| Görsel parite (F9, abonelik/ops) | ✅ aynı araç, eski 8080 vs geçici 4088, `--mask=#forgotNote`: **30/30 0 px**, duman 14/14 (e2e F9 öncesi ve `uyelik.hbs` düzeltmesi sonrası iki kez). **Maskesiz koşuda anonim 8 sayfa 24/24 0 px**; ham fark yalnız ADR-0003 istisna 4 (F6/F8 ile birebir aynı: uyelik 368 px × 3 viewport, sepet 368 px mobil/tablet, sepet desktop 13 581 px). F9 CMS bağlama yöntemi paritenin korunma nedenidir: `sepet.texts`/`uyelik.texts` render EDİLMEYEN `<template data-*>` içinde basılır, sayfa betiği metinleri çalışma anında `textContent` ile çeker ve anahtar boşsa markup'taki sabitin **aynısına** düşer → HTML çıktısı ve yerleşim değişmez |
| `@bagdam/admin` | ✅ tsc + eslint + vite build (`dist/app/*`) + **228 vitest (36 dosya)** (F5 ekranları 9–15 · F6 ekran 16 Müşteriler / Sistem › E-posta günlüğü / E-posta test formu · F8: ekran 17 Siparişler, Kuponlar, Ayarlar › Ödeme PayTR alanları · **F9: ekran 14b Teslimat tarihleri, 18 Ödeme Problemleri, 19 Abonelikler (liste + detay), 20 Teslimat Günü (ops yardımcıları: pick/packing/bulk/print), 21 Özet · F10: ekran 22 Sistem (sağlık kartı + 5 günlük sekmesi) + rota bazlı React.lazy kod bölme (86 chunk)**); dev `http://localhost:4011` (Vite ::1'e bağlanır), `/api` + `/assets` + `/uploads` proxy → 4010 (`ADMIN_API_PROXY` env ile hedef değişir — **tam URL verilmeli**, ör. `ADMIN_API_PROXY=http://127.0.0.1:4088`; yalnız port verilirse proxy sessizce başarısız olur) |
| Admin e2e (F4) | ✅ `tools/e2e-admin/run.mjs` — geçici API :4033 + `vite preview` :4034 (proxy'li): giriş → fiyat 480→485 (bootstrap + `/urun.html` anında) → parti ZY-12 güncel (batch/why) → PNG yükle → ürüne ekle → kapak (bootstrap `img=uploads/urunler/….webp`, `/uploads` statik 200) → Haftanın Kutusu sezon/small değiştir+Yayınla (bootstrap + `/kutu.html` templates) → kategori panelNote (admin API) → audit-logs (LOGIN/UPDATE/CREATE/PUBLISH/UPLOAD, actorEmail) → çıkış 401 + CSRF'siz POST 403 → **tüm değişiklikler geri alındı, bootstrap ≡ baseline**; 12/12 OK, rapor `tools/e2e-admin/report.md` |
| Admin e2e (F5) | ✅ `tools/e2e-admin/run-f5.mjs` — geçici API :4043 + `vite preview` :4044 (proxy'li): giriş → Site Blokları home.hero başlığı (RichTextLite HTML modu) → `/` HTML'inde anında → geri al · promoBar → `/index.html` + `/urunler.html` · Günlük: yeni yazı taslak (gunluk.html'de yok) → yayınla (gunluk.html + `/api/v1/posts` + ana sayfa son yazılar + sitemap) → sil · Yasal: KVKK yeni taslak sürüm → Yayınla (politikalar.html + `/api/v1/legal/kvkk` v2; yayındaki PUT 409 LEGAL_CURRENT_LOCKED; `/legal/kvkk/v/1` arşiv) → v1 yeniden yayınla · Toptan: `/toptan.html` formu 201 → admin listesi → CONTACTED → 4. istek 429 (3/dk/IP) · Bölgeler: Urla 49→55 → bootstrap + `/index.html` DELIVERY_FEE → 49; dates/generate idempotent (0 yeni) · Genel: commerce.freeShippingRule gte→gt → GET /admin/settings + bootstrap.commerce → gte · E-posta: SMTP parolası → GET maskeli+hasValue, DB `enc:v1:` (düz metin yok), maske PUT değiştirmez, test düğmesi 501→"F6" · audit-logs (content/settings/delivery/wholesale, `[redacted]`) · çıkış 401 → **tümü geri alındı (API + psql: toptan talepleri, KVKK v2 taslağı, mail.pass), site-content/legal/posts/bootstrap ≡ baseline**; 13/13 OK, rapor `tools/e2e-admin/report-f5.md` |
| e2e (F6, site + admin) | ✅ `tools/e2e-admin/run-f6.mjs` — geçici API :4053 (`WEB_URL`/`ADMIN_URL` geçici portlara; `DISABLE_MAIL=true`) + `vite preview` :4054 (proxy'li); site ve admin ayrı tarayıcı bağlamı: `/uyelik.html` üye ol (KVKK işaretli, pazarlama işaretsiz; KVKK'sız istemci engeli) → 201 + çerezler → yenileme → bootstrap `me` + hesap görünümü, çerezli HTML `private, no-store`, Consent satırları (KVKK true NOT_APPLICABLE / MARKETING_EMAIL false PENDING, yayındaki belgeye bağlı) → MailLog welcome+verify SKIPPED + `logs/mail/<id>.html` → verify bağlantısı 302 `?dogrulandi=1` → emailVerifiedAt + bilgi notu (tekrar tıklama idempotent; geçersiz imza `?dogrulandi=0`; biçimsiz token 400) → çıkış 204 (çerezler/refresh hash silindi) → yanlış parola mesajı · "parolamı unuttum" 200 + not (sha256 token DB'de; bilinmeyen e-posta da 200) · doğru parola → çıkış → reset önizlemesinden `?sifirla=` (adres çubuğundan silinir) → sıfırlama dalı → yeni parola 200 + anında giriş + flash (token tek kullanımlık 400; password-changed maili) → API: eski parola 401 / yeni 200, CSRF'siz PUT 403, `/me/consents` → adres formu (ilçe select `/delivery/zones`, Urla) PUT 200 → özet → GET `/me/address`; düzenleme aynı satırı günceller (zip boşaltma → null) → `sepet.html` oturumlu: giriş kapısı açık, formlar oturum/adresten dolu, teslimat adımı açılır → `toptan.html` oturumlu form 201 (X-CSRF-Token) → admin: Müşteriler (arama/rol rozeti/Doğrulandı/son giriş) → detay (onaylar KVKK Verildi / pazarlama Reddedildi + belge sürümü, adres, audit özeti; ad PATCH; SELF_DEACTIVATE 400) → Anonimleştir (e-posta `anon+<id>@anon.local`, ad/telefon/adres silindi, isActive false, müşteri 401×3, tekrar 409; onaylar korunur) → Sistem › E-posta günlüğü 4 satır SKIPPED (API previewPath) → Ayarlar › E-posta test gönder → SKIPPED + önizleme → audit-logs (REGISTER/PASSWORD_RESET/LOGIN/LOGOUT, me:UPDATE, customers:UPDATE/ANONYMIZE, settings; e-posta redakte, parola yok) → çıkış 401 → **temizlik: kullanıcı/consent/adres/mail_logs/audit/önizleme dosyaları/toptan talebi silindi → sayımlar ≡ başlangıç**; **19/19 OK**, rapor `tools/e2e-admin/report-f6.md` |
| e2e (F8, site + admin + PayTR bildirimi) | ✅ `tools/e2e-admin/run-f8.mjs` — geçici API :4074 (`PAYMENT_PROVIDER=manual`, `ENABLE_CRON=false`, `DISABLE_MAIL=true`, `WEB_URL`/`ADMIN_URL` geçici portlara) + `vite preview` :4075 (proxy'li); site ve admin ayrı tarayıcı bağlamı: **a** başlangıç sayımları + `delivery_dates.reserved` anlık görüntüsü + `payment.*` satırları saklanır → **b** panelden kupon (%10, SINGLE) → **c** `POST /auth/register` + `PUT /me/address` (urla) → **d** `/urun.html?id=ekmek` sepete ekle → `/sepet.html` giriş kapısı → giriş → adımlar açılır, form adresten dolu → **e** teslimat günü (`GET /delivery/dates`) + yasal onay kutuları (`on-bilgilendirme` + `mesafeli-satis`; tekilde abonelik sözleşmesi İSTENMEZ) + buton metni "ödeme yükümlülüğü doğurur" + kupon uygula → özet satırları `POST /checkout/quote` ile birebir (toplam/kargo/indirim) → **f** "siparişi tamamla" → manual sağlayıcı → Order PAID, Payment `ord<no>`+4 SUCCEEDED, sepet+taslak temizlendi → **g** `/sepet.html?siparis=<no>&odeme=ok` (PayTR dönüş dalı) → teşekkür + sorgu adres çubuğundan silinir → **h** `/uyelik.html` "önceki siparişler" + `/me/orders` + `/orders/:no/status` + `mail.order-paid` SKIPPED (önizlemede sipariş no + yasal belge bağlantısı) → **i** admin Siparişler arama → detay (satır/müşteri/kupon) + CouponRedemption 1 + `usedCount` 1 → **i2** Ayarlar › Ödeme (PayTR alanları registry'den + "manuel sağlayıcı" uyarı şeridi; sırlar maskeli, düz metin YOK) + Özet "Bugün — sipariş ve ciro" kartı → **j** panelden Hazırlanıyor → Yolda → Teslim edildi → **k** `GET /admin/orders/export.csv` → **l** `POST /admin/payments/:id/refund` (ManualProvider) → Payment REFUNDED + Order REFUNDED + kupon kullanımı geri (redemption 0, usedCount 0) → **m** `/kutu.html?tier=sezon` taslağı → sepet → SUBSCRIPTION_CONTRACT_ACK → Order SUBSCRIPTION PAID + Subscription ACTIVE + cycle#1 `prepaidAmount = grandTotal − shippingFee` → `/me/subscription` → **n** iptal akışı (talep → kalma teklifi %50×1 kabul → ikinci talep teklifsiz → confirm) → CANCELLED, `/me/subscription` null → **o** PayTR bildirimi: `payment.*` test değerleri + psql ile PENDING PAYTR ödemesi → geçersiz hash **400** (WebhookEvent yazılmaz) · IP allowlist dışı **403** (ödeme PENDING kalır) · geçerli hash **200 "OK"** → Payment SUCCEEDED + Order PAID + WebhookEvent PROCESSED · ikinci teslim **IGNORED** (tek WebhookEvent) → **z temizlik: 16 tablo sayımı ≡ başlangıç, `delivery_dates.reserved` geri, `payment.*` satırları geri**; **17/17 OK**, rapor `tools/e2e-admin/report-f8.md` |
| e2e (F9, site + admin + ops) | ✅ `tools/e2e-admin/run-f9.mjs` — geçici API :4088 (`PAYMENT_PROVIDER=manual`, `ENABLE_CRON=false`, `DISABLE_MAIL=true`, `WEB_URL`/`ADMIN_URL` geçici portlara) + `vite preview` :4089 (`ADMIN_API_PROXY=http://127.0.0.1:4088`); site ve admin ayrı tarayıcı bağlamı; zaman yalnız `POST /admin/jobs/:name/run {now}` ile ilerletilir: **a** 16 tablo sayımı + `delivery_dates.reserved` + `commerce` ayarları saklanır (dunning [2,12] · `firstCycleSkippable` true · `deliveryDatesHorizonWeeks` 2) → **b** sonraki 4 haftaya BoxTemplate kopyala + yayınla → **c** müşteri A kaydı + adres + saklı kart (`ok:`) → **d** `/kutu.html` "aboneliği başlat" → `/sepet.html` gün + `SUBSCRIPTION_CONTRACT_ACK` → Order SUBSCRIPTION PAID · Subscription ACTIVE · cycle#1 SCHEDULED → **e** `/uyelik.html` abonelik kartı (kutu içeriği, "Bu haftaki ödeme", `DEĞİŞİKLİK İÇİN … SÜREN VAR` geri sayımı) → **f** teslimat günü değiştir (cycle#1 aynı hafta taşınır, DD rezervi −1/+1) → **g** `/kutu.html` canlı mod: `?tier=` yok sayılır + tür düğmeleri disabled (ADR-0008), swap + ürün tercihi + ekstra → onayla, frekans 1hafta↔2hafta → **h** `/sepet.html` "bu haftaki kutuma ekle" → `CART_MERGE` satırı, sepet boşalır → **i** haftayı atla → geri al (SKIPPED ↔ SCHEDULED, reserved 1→0→1) → **j** kesim (`cycles:lock-and-charge {now}`) → cycle#1 CHARGED → **k** iptal akışı: talep → kalma teklifi (`{boxes}/{pct}` dolu) → "iptalden vazgeç" → ACTIVE → tekrar talep (teklifsiz) → onayla → CANCELLED (`/me/subscription` null; kilitli cycle CHARGED kalır, ADR-0007) → **l** ekran 19 liste + detay (künye, kutu geçmişi, 17 tür olay, 2 iptal kaydı) → **m** ekran 20: toplama listesi (ürün adı + tercih), paketleme fişi (müşteri/telefon/adres/içerik), yazdırma görünümü (`print` medyası: `.print-only` görünür, `.no-print` gizli), toplu durum PREPARING → OUT_FOR_DELIVERY → DELIVERED → **n** müşteri B + `fail:` kart + admin manuel checkout + `cycles:ensure` → **o** kesim → cycle#2 **UNPAID** (`MANUAL_DECLINED`) → **p** ekran 18: satır listede (kutu + sipariş satırı), kart düzeltilir, "yeniden çek" → CHARGED, liste 2 → 0 → **q** ekran 14b: kapasite 999→998→999, günü kapat/aç, "Tarih üret" +0 (idempotent) → **r** ekran 21 Özet kartları `GET /admin/dashboard` ile birebir → **z temizlik: 16 tablo sayımı ≡ başlangıç, `delivery_dates.reserved` geri, `commerce` ayarları geri**; **19/19 OK**, rapor `tools/e2e-admin/report-f9.md` |
| Görsel parite (F10, çerez şeridi) | ✅ aynı araç, eski 8080 vs geçici 4094, `--mask=#forgotNote,#cookieConsent`: **30/30 0 px**, duman 14/14 — `SEED_OVERWRITE_CONTENT=true pnpm db:seed` (16 mail şablonu + KVKK "Saklama süreleri" paragrafı DB'ye indi) ve CMS zengin metin temizleyicisi (D) sonrası. Çerez şeridi **11. ADR-0003 istisnasıdır**: 10 sayfada basılır ama `style="display:none"` + `position:fixed` ile sayfa akışına girmez → maskesiz koşuda da anonim görünümü değiştirmez; maske yalnız JS ile açıldığı durumu ölçüm dışında tutmak için. KVKK metnine eklenen paragraf `politikalar.html`'de **gizli** `<article>` içindedir (kvkk sortOrder 5, ilk makale değil) → görünür yerleşim değişmedi. |
| e2e (F10, site + panel + job + güvenlik) | ✅ `tools/e2e-admin/run-f10.mjs` — geçici API :4094 (`PAYMENT_PROVIDER=manual`, `ENABLE_CRON=false`, `DISABLE_MAIL=true`) + `vite preview` :4095 (`ADMIN_API_PROXY=http://127.0.0.1:4094`; panel adresi **`http://localhost:4095`** — vite ::1 dinler): **a** 17 tablo sayımı + `delivery_dates.reserved` + `privacy`/`cookies` ayarları saklanır, çerez kategorileri açılır → **b** 10 sayfada şerit sunucuda gizli → **c** "Reddet" → Consent ×2 `granted=false` + `bagdam_cookie_consent` + yeniden yüklemede şerit yok → **d** "Yönet" (analitik açık / pazarlama kapalı) → "Seçimimi kaydet" → true/false → **e** "Kabul Et" ikisi de true; pazarlama ayarı kapatılınca kutucuk markup'a hiç girmez ve onayı yazılmaz → **f0/f** 4 haftalık şablon + müşteri A (adres + `ok:` kart) + admin manuel checkout (MIT) + `cycles:ensure` → **g** `reminders:cutoff` (kesim −23,5 s) → MailLog `cutoff-reminder` (entityId = cycleId); önizlemede 6 kutu ürünü + teslimat günü + atlama bağlantısı; ikinci koşu **yeni satır üretmez** → **h** `cycles:lock-and-charge` → cycle CHARGED + `cycle-charged` (tutar + sipariş no) → **i** müşteri B `fail:` kart → cycle UNPAID + `cycle-payment-failed` (`entityId = cycleId:1`, kart güncelle bağlantısı) → **j** panelden `Hazırlanıyor → Yolda → Teslim edildi` (`order-shipped`, `order-delivered`) + B siparişi gerekçeli `Teslim edilemedi` (`order-delivery-failed`, gerekçe önizlemede) → **k** `/me/subscription/cancel` + confirm → CANCELLED + `subscription-cancelled` → **l** `kvkk:purge`: 120/60/200 gün eski MailLog (+ önizleme dosyası) / SystemLog / CronLog **silinir**, 400 gün eski AuditLog satırı **durur** ama `actorEmail`/`ipAddress`/`summary`/`newValues` PII'si `[silindi]` olur (PII olmayan alan korunur), taze satırlar etkilenmez → **m** ekran 22: `GET /admin/health/detailed` (db up, mailDisabled, jobRunAllowed) + Denetim/Sistem/Cron/E-posta/Webhook sekmeleri dolu → **n** güvenlik: 6 admin ucu oturumsuz 401 · müşteri `/admin/*` 403 · başkasının siparişi 404 · CSRF'siz PATCH 403 (`code=CSRF_INVALID`) · CSP web (PayTR `frame-src`, `font-src data:`) / api (`default-src 'none'`) · dev'de HSTS yok · `X-Powered-By` yok · sır yazıldı → GET maskeli, yanıtta `enc:v1:` yok, DB'de `enc:v1:` → **z temizlik: 17 tablo ≡ başlangıç, reserved geri, cookies/privacy ayarları geri**; **16/16 OK**, rapor `tools/e2e-admin/report-f10.md` |
| Handlebars şablonları | ✅ `node tools/hbs-check.mjs` — **19/19** derleniyor (12 sayfa + 7 partial; `{{> ad}}` referansları `hbs` paketinin `-`→`_` adlandırmasıyla denetlenir), eksik partial yok |
| Bağımlılık taraması | ⚠️ `pnpm audit` → 3 high: `sharp@0.34.5` (**çalışma zamanı**, libvips CVE'leri — F10b'de ≥0.35'e yükselt), `deepmerge-ts@7.1.5` + `effect@3.18.4` (yalnız `@prisma/config` → Prisma CLI zinciri, üretimde yüklenmez). Ayrıntı `docs/guvenlik-denetimi.md` §8 |
| Medya içe aktarma | ✅ `pnpm --filter @bagdam/api media:import` → tarandı 85 · media_files 85 (ikonlar 27 · logo 5 · sahne 13 · urunler 40); ikinci koşu 0 değişiklik (idempotent) |

## Sunucu

> Coming-soon / apex yayını bu çalışmanın kapsamı dışında (kullanıcı ayrı çalışmada yürütüyor).

ADR-0017: sunucu kurulumu ve yayın **F10b**'de (lansmandan hemen önce). `deploy/README.md` runbook'u hazır. Portlar 5010 (prod) / 5011 (staging) ayrılmış durumda.

## Açık ürün kararları (kuyruk ≤3 — ADR-0016)

Kapatılanlar (2026-08-20): aşağıdaki üç madde **→ ayara taşındı (ADR-0018); varsayılan: ≥ / kuruş / evet; admin F5'te değiştirilebilir** (Setting `commerce.freeShippingRule` / `commerce.discountRounding` / `commerce.subscriberFreeShipping`; pricing `PricingContext.rules`).
- ~~Ücretsiz kargo eşiği `≥ 1000` mi `> 1000` mi~~ → `freeShippingRule` (`gte` varsayılan | `gt`).
- ~~İlk-2-kutu/retention indirimi 324,50 mi 325 mi~~ → `discountRounding` (`kurus` varsayılan | `tl`).
- ~~Aktif abonenin tekil ürün siparişinde kargo 0 mı zone kuralı mı~~ → `subscriberFreeShipping` (`true` varsayılan | `false`; abonelik siparişinde kargo her zaman 0).

Kapatılan (2026-08-21, F8): ~~**Kupon sistemi MVP'ye alınsın mı?**~~ → **evet, MVP'de** — `modules/coupons` (PERCENT|AMOUNT · ALL|SINGLE|BOX · minSubtotal · tarih aralığı · usageLimit/perUserLimit), `PricingService.quoteWithCoupon` uygular (yuvarlama ADR-0018), `CouponRedemption` ödeme PAID olunca aynı işlemde yazılır (`usedCount++`), iptal/iade geri alır; admin "Kuponlar" ekranı + sepette kupon alanı (ADR-0003 istisna adayı 8).

Kuyrukta kalan: **yok** (≤3 kuralı sağlanıyor). Karar bekleyen teknik notlar aşağıdaki "F8 notları" başlığında.

## Bilinen açık notlar (F1 ajan raporlarından, sonraki fazlarda ele alınacak)

- **F10 notları (2026-08-21, D entegrasyonu):**
  - **`sharp@0.34.5` güvenlik yükseltmesi bekliyor (F10b):** `pnpm audit` 3 high veriyor; yalnız `sharp` çalışma zamanındadır (libvips CVE-2026-33327/33328/35590/35591). Etki yüzeyi panel medya yükleme (ADMIN/STAFF, 20 MB, mime allowlist) — anonim kullanıcı sharp'a girdi veremez. `pnpm add` bu görevde yasaktı → `pnpm --filter @bagdam/api add sharp@^0.35` F10b/F11 kontrol listesinde. `deepmerge-ts` + `effect` yalnız `@prisma/config` (CLI) zincirinde, üretimde yüklenmez.
  - **CMS zengin metni artık sunucuda temizleniyor (B'nin açık notu kapandı):** `common/security/html-sanitize.ts` reddeden (deny-list) bir temizleyicidir — `<script|iframe|object|applet|frameset>` gövdesiyle, `<base|link|meta|embed|frame>` etiketiyle silinir; `on…=`, `srcdoc`, `formaction`, `xlink:href` düşer; `javascript:`/`vbscript:`/görsel olmayan `data:` bağlantıları `#` olur. **İki yerde** çalışır: yazma (`ContentAdminService` — site-content richtext, Post `titleHtml/bodyHtml`, LegalDocument `leadHtml/bodyHtml`) ve render (`web/content-view.ts`, `escapeContentValue`). Temiz içerik **byte-byte aynı** kalır (DB'deki 315 HTML dizesinde 0 değişiklik ölçüldü) → parite bozulmaz. `<style>` ve `style="…"` **bilerek** kapsam dışı (CSS enjeksiyonu XSS değil, CMS'te meşru kullanımı var). Tam bir HTML ayrıştırıcısı değil; asıl savunma katmanı CSP değil bu süzgeçtir çünkü web CSP'si ADR-0003 gereği `script-src 'unsafe-inline'` taşımak zorunda.
  - **Çerez şeridi ADR-0003'ün 11. istisnasıdır** ve metne işlendi. Parite koşusu artık `--mask=#forgotNote,#cookieConsent` ile yapılır; maske olmadan da anonim görünüm aynıdır (şerit sunucuda `display:none` + `position:fixed`), maske yalnız JS'in şeridi açtığı durumu ölçüm dışında tutar.
  - **`privacy` ayar grubu panelde:** `AdminAyarlarGenelPage` `groups={['commerce','site','cookies','privacy']}` (A'nın bıraktığı tek satırlık iş). Grup DB'de satır oluşturmaz — değer girilene kadar `PRIVACY_SETTINGS_DEFAULTS` kullanılır (120/90/30/90/12/0).
  - **KVKK "Saklama süreleri" paragrafı artık DB'de:** `SEED_OVERWRITE_CONTENT=true pnpm db:seed` koşuldu (site_content 40 · legal_documents 11 · posts 3 ezildi), ardından düz `pnpm db:seed` **idempotent** doğrulandı. Paragraf `politikalar.html`'de KVKK makalesindedir; KVKK `sortOrder 5` olduğu için sayfa açılışında **gizli** `<article>` içinde kalır → parite 30/30 0 px korundu. Staging/prod'da F10b'de aynı komut gerekir.
  - **`reminders:cutoff` penceresi dar:** yalnız `cutoffAt ∈ (now+23s, now+24s]` olan **SCHEDULED** cycle'lar taranır (`CUTOFF_REMINDER_HOURS_BEFORE=24`, `CUTOFF_REMINDER_WINDOW_MS=1s`). Cron saatte bir koşar; **bir koşu kaçarsa o cycle hatırlatması gitmez** (telafi yok). Sunucuda job'un saatlik koştuğu `cron_logs`'tan izlenmeli (ekran 22 › Cron).
  - **MailLog tekilliği bildirim başına anahtar seçer:** `cutoff-reminder` → `cycleId` (cycle başına bir kez), `cycle-charged` → `cycleId`, `cycle-payment-failed` / `cycle-awaiting-payment` → `cycleId:attemptNo` (her deneme ayrı satır), `order-*` → `orderId`, `subscription-cancelled` → `subscriptionId`, `subscription-past-due` → `subscriptionId:failedCycles`. `@@unique([templateSlug, entityId])` olduğu için aynı anahtar ikinci kez gönderilirse **satır ezilir** (F6 notu) — `sendOnce` bunu bilerek kullanır.
  - **e2e F10 panel adresi `http://localhost:4095`** olmalı: `vite preview` IPv6 (`::1`) dinler, `127.0.0.1:4095` bağlantı vermez (F9 notunun ikizi).
  - **psql'e Türkçe karakter geçirmek:** Windows'ta `execFileSync` argümanları ANSI kod sayfasıyla kodlar → `invalid byte sequence for encoding "UTF8"`. `run-f10.mjs` ASCII dışı sorguları UTF-8 geçici dosyaya yazıp `psql -f` ile çalıştırır (repo yolunda `ı` olduğu için önizleme yolu bile ASCII değil).
  - **`nginx location /app/` hâlâ eksik** (C'nin notu): `deploy/nginx/admin.bagdam.com.conf` yalnız `/assets/` için cache tanımlıyor, admin çıktısı `dist/app/*`. Kod bölmeden sonra 86 chunk önbelleksiz iner. `deploy/` bu görevin yazma kapsamı dışıydı → **F10b** ve `docs/RUNBOOK.md` §8 kontrol listesinde.
  - **`script-src-attr 'none'` riski açık:** PayTR `checkoutFormContent` dalı (sepet.hbs `injectHtml`) sağlayıcıdan gelen HTML'de inline `onclick=`/`onload=` kullanırsa Chrome engeller. Şu an kullanılan yol `redirectUrl`/`token` → iFrame, etkisiz; **F10b/F11 sandbox provasında** doğrulanmalı.
  - **Şema donduruldu (ADR-0021):** 37 model / 29 enum / 5 migration; bundan sonra yalnız additive migration. Kolon silme/yeniden adlandırma/tip daraltma yeni ADR ister.
- **F9 notları (2026-08-21, E entegrasyonu):**
  - **`uyelik.hbs` "atlamayı iptal et" düzeltmesi (E):** atlanan hafta müşteri DTO'sunda `currentCycle.locked = true` gelir (`toClientCycleSummary`: `locked = kesim geçti || status !== 'SCHEDULED'`), sayfa da `skipDisabled` içinde `!editable` kontrolü yaptığı için **geri alma düğmesi kalıcı olarak pasif kalıyordu** (sunucu `DELETE …/cycles/current/skip` kabul ediyor). Düzeltme: geri alma yalnız kesim anına bakar (`cutoffAtIso` vs `BahcedenCart.serverNow()`), kutu içeriğini düzenleme kilitli kalır. Markup değişmedi (parite etkilenmez).
  - **`sepet.texts` / `uyelik.texts` bağlama yöntemi:** CMS değerleri render EDİLMEYEN `<template id="sepetTexts|uyelikTexts" data-*="{{{site.*}}}">` içinde basılır; sayfa betiği `T(key)` ile okur, anahtar boşsa markup ile **birebir aynı** varsayılana (`SEPET_TEXT_DEFAULTS` / `SUB_TEXT_DEFAULTS`) düşer → panelden içerik değiştirilmediği sürece anonim HTML/yerleşim değişmez, parite 30/30 0 px korunur. `{no}` / `{belge}` / `{boxes}` / `{pct}` yer tutucuları JS'te doldurulur. Sepet'teki manuel sağlayıcı notu ("Test ödeme onaylandı — ") CMS'e **alınmadı**, sayfa-yerel sabittir (e2e F8 beklentisi korunsun diye).
  - **Yeni içerik anahtarları DB'ye düz `pnpm db:seed` ile İNMEZ:** seed mevcut `site_content` satırlarını korur (şema tazeler, değer ezmez). `uyelik.texts`'e F9'da eklenen 5 alan (`skipYes`, `skipNo`, `discardLabel`, `cancelBack`, `busyNote`) ve `sepet.texts` bloğu lokalde **`SEED_OVERWRITE_CONTENT=true pnpm db:seed`** ile indirildi (DB'de 49/29 alan doğrulandı). Staging/prod'da F10b'de aynı komut gerekir; koşulmazsa sayfa kendi varsayılanına düşer (kırılma yok, yalnız panelden düzenlenemez).
  - **Ops listelerinin kapsamı:** `pick-list`/`packing-list` yalnız **CHARGED · PREPARING · OUT_FOR_DELIVERY** cycle'ları kapsar (ödemesi alınmış kutular); `day-summary` tüm durumları sayar. Checkout'tan çıkan cycle#1 **SCHEDULED**'dır → kesim (`cycles:lock-and-charge`) koşmadan ops listelerinde görünmez. e2e bunu job'a `now` vererek sürer.
  - **Aynı sipariş Ödeme Problemleri'nde iki satır üretebilir:** tahsil edilemeyen cycle hem `CYCLE` (UNPAID) hem de ilişkili Order `PAYMENT_FAILED` olduğu için `ORDER` satırı olarak listelenir. Panelde ikisi de aynı müşteriyi gösterir; "yeniden çek" yalnız cycle satırında çıkar ve başarılı olunca iki satır da düşer. Birleştirme (tek satır) istenirse `PaymentIssuesService` filtresi F10'da daraltılabilir.
  - **DELTA tahsilatı saklı kart ister:** kesimden önce eklenen ekstra / "kutuma ekle" satırları cycle toplamını peşin tutarın üstüne çıkarır → kilitte DELTA Order üretilir. Abonelikte saklı kart yoksa `ChargeStrategyResolver` **PAYMENT_LINK**'e düşer ve cycle `AWAITING_PAYMENT` olur (ops listelerine girmez). PayTR kart saklama teyidi gelene kadar (F8 notu) canlıda beklenen davranış budur; e2e A müşterisine `ok:` kart bağlayarak MIT kolunu sürer.
  - **`commerce.firstCycleSkippable` varsayılanı `false`:** ilk kutu atlanamaz (409 `FIRST_CYCLE_NOT_SKIPPABLE`). e2e atla/geri al adımı için ayarı koşu süresince `true` yapar ve geri koyar. Lansman kararı ayarda (ADR-0018 kalıbı).
  - **Ops/abonelik ekranlarının e2e'si zaman ilerletir:** `run-f9.mjs` `cycles:lock-and-charge`'ı gelecekteki `now` ile çağırır → o ana kadarki **tüm** SCHEDULED cycle'lar kilitlenir. Jest suite'leri ve e2e aynı `bagdam_dev` üzerinde **aynı anda koşmamalı** (F7/F8 notuyla aynı kural).
  - **`ADMIN_API_PROXY` tam URL ister:** `vite.config.ts` değeri doğrudan proxy `target`'ına verir; `ADMIN_API_PROXY=4088` gibi yalnız port verilirse panel API'ye ulaşamaz (giriş sayfasında takılır). Doğrusu `ADMIN_API_PROXY=http://127.0.0.1:4088`.
  - **Tek seferlik (isOneTime) kutu yönetimi** F9 e2e'sinde sürülmedi (F7 e2e `d/e` adımları ve jest kapsıyor: COMPLETED'a geçiş, `/me/subscription` null). Sayfa tarafında `uyelik.hbs` `completed` dalı (rozet + not + "yeniden sipariş" CTA) yalnız birim düzeyinde doğrulandı.
  - **`GET /admin/payment-issues` DTO'su** `{items,total,page,limit,counts{failedOrders,unpaidCycles,awaitingPaymentCycles,total}}`; toplu durum gövdesinde panel `skipInvalid: true` gönderir (kısmi başarı + uyarı toast'ı).
- **F8 notları (2026-08-21, E entegrasyonu):**
  - ~~**ADR-0003 istisna listesi güncellenmeli**~~ → **F9'da kapandı: `docs/adr/0003-frontend-stratejisi.md` "İzinli tasarım istisnaları (10; F8 sonrası güncel)" olarak işlendi; F9 YENİ istisna eklemedi** (CMS metinleri render edilmeyen `<template data-*>` ile geldiği için markup değişmedi). Eski not: ADR-0003 "izinli tasarım istisnaları (7)" listesi F8 sonrası fiilen **10**: (1) ~~iyzico CF konteyneri~~ → **PayTR iFrame konteyneri** (ADR-0019; `#paymentFrame`, prototipteki 4 alanlı kart formunun yerinde, aynı kutu/boyut) · (2) üye ol KVKK+pazarlama · (3) checkout belge onayı + "siparişi tamamla — ödeme yükümlülüğü doğurur" (`#checkoutLegal`) · (4) "parolamı unuttum" · (5) adres ilçe select · (6) coming-soon/404/bakım · (7) uyelik abonelik durum metinleri · **(8) sepet özetinde kupon alanı** (`#couponRow`: mevcut `.checkout-form` stiliyle etiket+input+`.btn`) · **(9) ödeme adımında saklı kart seçim listesi** (`#savedCards`, kart yoksa gizli) · **(10) uyelik "kartlarım" kartında 4 alanlı kart formu yerine "yeni kart ekle" düğmesi + not** (`#addCardBtn`/`#cardMsg`; PayTR'de ayrı kart ekleme yok → 501 "Kart ilk ödemede saklanır"). Ayrıca hata/bilgi satırları `#checkoutMsg`/`#paymentMsg`/`#couponMsg`/`#addrMsg` mevcut `.checkout-auth-msg` kalıbıyla ve varsayılan `hidden`. **Bunların hiçbiri anonim görünümde çizilmez → parite 30/30 0 px.** `docs/adr/0003-*.md` bu görevin yazma kapsamında değildi; listenin ADR'ye işlenmesi gerekiyor.
  - **prepaidAmount (F7 açık notu KAPANDI):** checkout `prepaidAmount = kutu + ekstralar − indirim`, **kargo HARİÇ**; kargo `Order.shippingFee`'de. Abonelik siparişinde kargo zaten 0 (ADR-0005), tek seferlik kutuda zone kuralı uygulanır ama DELTA hesabına girmez → DELTA'da kargo çift tahsil edilmez. `docs/state-machines.md` §12 ile tutarlı.
  - **Kupon indirimi cycle#1 `prepaidAmount`'tan DÜŞÜLMEZ** (motor `total − prepaid` hesabı kuponu bilmez; DELTA'da kupon geri verilmesin diye). Sonuç: abonelik ilk kutusunda kupon kullanılırsa iptal `refundAmount`'ı kupon kadar fazla görünür → iade admin panelinden elle. Kupon yalnız checkout Order'ına yansır.
  - **Hata zarfı alanları:** `AllExceptionsFilter` yalnız `message`/`error` taşır → `CONSENT_REQUIRED` eksik türleri mesaj metninde ("…eksik: PREINFO_ACK, …"), `CHECKOUT_IN_PROGRESS` / `PAYMENT_INIT_FAILED` sipariş no mesajda (`#no`). İstemcinin ayrıştırabileceği ayrı alan isteniyorsa filter genişletilmeli (F9).
  - **PayTR kart saklama teyidi bekliyor:** iFrame API dokümanında `store_card`/`utoken` parametresi YOK (yalnız Direkt API'de). `store_card=1` yalnız `saveCard && Setting payment.storedCardEnabled` iken gönderilir; PayTR'den yazılı teyit alınana kadar `storedCardEnabled=false` kalmalı → `ChargeStrategyResolver` abonelik tahsilatını **PAYMENT_LINK**'e düşürür (ADR-0019). Aynı şekilde `nonThreeDsGranted` (NON3D/MIT yetkisi) F11 checklist maddesi.
  - **PayTR canlı/test API'sine hiç istek atılmadı** (mağaza bilgisi yok): sağlayıcı testleri `PAYTR_HTTP` mock'uyla, e2e yalnız **gelen** bildirim ucunu sahte imzalı gövdeyle sürer. Gerçek uçtan uca (3DS + kart saklama + iade) **staging sandbox'ta F10b/F11'de** doğrulanacak — F8 "Bitti sayılır" ölçütünün bu kalemi açık.
  - **`payments:reconcile` (F7 açık notu KAPANDI):** `@Cron */15` → `CheckoutCompletionService.reconcile` — 30 dk'dan eski açık (PENDING/REQUIRES_3DS) CHECKOUT/LINK ödemeleri sağlayıcıdan sorgulanır; PayTR'de sorgu yoksa 24 saat sonra Payment EXPIRED + Order CANCELLED + DeliveryDate iade. `docs/state-machines.md` §14 #8 bu kararla kapandı.
  - **Manuel sağlayıcı koruması:** `ManualProvider` yalnız çözümlenen sağlayıcı adı `manual` iken devreye girer (Setting `payment.provider` → env `PAYMENT_PROVIDER` → varsayılan). Üretimde `manual` çözümlenirse `PaymentProviderFactory.getActive()` süreç başına bir kez ERROR log'u yazar ("gerçek tahsilat YAPILMAZ") — F11 go-live checklist maddesi (`payment.provider = paytr`, `paytrTestMode = false`).
  - **`GET /delivery/dates` DTO'sunda `id` yok:** checkout hem `deliveryDateId` hem `deliveryOn` (YYYY-MM-DD, adres bölgesi) kabul eder; `sepet.hbs` seçili tarihte `id` varsa onu, yoksa `deliveryOn` gönderir. DTO'ya `id` eklemek F9'da düşünülebilir (şu an gerek yok).
  - **Ayar önbelleği (e2e notu):** `SettingsService` grup satırlarını 60 s önbellekler. e2e temizliği `payment.*` satırlarını psql ile sildiği için çalışan API süreci en fazla 60 s daha eski PayTR test değerlerini bellekte tutabilir (DB temiz). Koşudan hemen sonra ayarları panelden doğrulayacaksan API'yi yeniden başlat.
  - **`checkoutState.provider` düzeltmesi (E):** `sepet.hbs` sağlayıcıyı `payment.provider` (Prisma enum `MANUAL`) ile karşılaştırıyordu; sözleşme `payment.providerName` (`manual`|`paytr`) diyor → `providerName || provider` küçük harfe indirgenerek okunuyor. Belirti: manuel sağlayıcıda "Test ödeme onaylandı" notu çıkmıyor, PayTR olmayan akışta iFrame konteyneri açılmaya çalışılıyordu.
  - **Testler/e2e birlikte koşmaz:** F7 notundaki kural F8 için de geçerli — jest suite'leri ve `run-f8.mjs` aynı `bagdam_dev` üzerinde çalışır; e2e ayrıca `payment.*` ayar satırlarını koşu süresince değiştirir (sonda geri koyar).
  - **Kart ekleme ucu (F7 açık notu KAPANDI, kısmen):** `POST /me/cards/add-session` → 501 `NOT_IMPLEMENTED` ("Kart ilk ödemede saklanır"); `GET /me/cards` + `DELETE /me/cards/:id` (soft) çalışıyor. Gerçek kart ekleme oturumu PayTR kart saklama teyidi gelirse F9/P2.
- **F7 notları (2026-08-20, E entegrasyonu):**
  - **Prisma client:** `pnpm db:generate` Windows'ta çalışan dev API (:4010) `query_engine-windows.dll.node`'u kilitlerken EPERM verebilir (A notu); şema/engine sürümü aynı, yalnız client klasöründeki `schema.prisma` kopyası eskimiş olabilir → dev sunucu yeniden başlatılınca `pnpm db:generate` tekrar.
  - **Ödeme sağlayıcısı:** ADR-0019 ile birincil sağlayıcı PayTR (iyzico P2); F7 yalnız `ManualProvider` (+ `PaymentProviderFactory`: Setting `payment.provider` → env `PAYMENT_PROVIDER` → manual). F8 `PayTrProvider` aynı arayüze takılır; `iyzico` seçilirse 503 `PAYMENT_PROVIDER_UNAVAILABLE`.
  - ~~**Checkout yok (F8):**~~ → **F8'de kapandı** (`modules/checkout`; aşağıdaki F8 notlarına bakın). Eski not: tek açılış yolu admin `POST /admin/subscriptions` (ManualCheckoutService — ofis/havale/nakit; yalnız ADMIN; MANUAL ödeme SUCCEEDED + Subscription ACTIVE). F8 checkout aynı orkestrasyonu (quote → createFromQuote → Payment → createFromCheckout → activate) sağlayıcı ödemesiyle kurar; kupon/consent bağları F8. Müşteri tarafında kart ekleme ucu yok → kart değişimi `PATCH /me/subscription {paymentMethodId}` (mevcut PaymentMethod; add-session F8).
  - ~~**Peşin tutar (prepaidAmount):**~~ → **F8'de karara bağlandı: kargo HARİÇ** (F8 notları). Eski not: manuel checkout'ta `quote.grandTotal` (kargo dahil) yazılır — tek seferlik kutuda kilit anı quote'u kargoyu içerdiğinden due 0 olur. Shared `PricingResult.prepaidAmount` (kargo hariç Σ BOX+EXTRA−indirim) karışık sepet için düşünülmüştü; F8 checkout'ta kutu+tekil ürün karışık sepette kargonun hangi tarafa yazılacağı kararlaştırılmalı (aksi hâlde DELTA'da kargo çift tahsil edilir).
  - **Dunning:** §14 #1 kararı koda yansıdı (08:00 sınırı; LINK kolu da sınırda SKIPPED(UNPAID)); varsayılan `commerce.dunning.retryHours [24,72]` ile kesimdeki ilk başarısız MIT doğrudan SKIPPED(UNPAID) → lansmandan önce admin'den `[2,12]` girilmeli (ADR-0006 değeri "24/72 saat" olarak kaldı — yeni ADR/ek not gerekli).
  - **Job zamanı ezmesi:** `POST /admin/jobs/:name/run {now}` yalnız `NODE_ENV !== production` ya da `ALLOW_JOB_TIME_OVERRIDE=true` iken; üretimde 403 `JOB_NOW_OVERRIDE_FORBIDDEN`. Staging'de simülasyon gerekirse env ile açılır (dikkat: ileri tarihli `now` gerçek cycle'ları kilitler).
  - **DeliveryDatesService.findOrCreateFor** Setting `commerce.deliveryDays` dışındaki güne 400 `NOT_DELIVERY_DAY` verir → admin bir teslimat gününü ayarlardan kaldırırsa o güne bağlı aboneliklerde `cycles:ensure` hata sayar (CronLog errors) — ops uyarısı/otomatik gün taşıma yok (karar bekliyor).
  - **Açık tahsilat kuyruğu:** LOCKED kalmış (çökme) cycle'lar 15 dk sonra `lock-and-charge` içinde yeniden tahsile girer; ~~Payment PENDING kalmış satırlar için `payments:reconcile` yok~~ → **F8'de eklendi** (`@Cron */15`, 30 dk / 24 s kuralı).
  - **SubscriptionEvent:** `OrdersService.transition` ADMIN/OPS aktörlü geçişlerde ADMIN_NOTE(`kind:'ORDER_STATUS'`) yazar; motor CHARGED/PAYMENT_FAILED/CANCELLED olaylarını kendisi yazar → `CYCLE_CREATED`/`CANCEL_ABANDONED`/`ORDER_STATUS` ayrı enum değerleri §14 #6 ile birlikte kararlaştırılacak (şimdilik `data` ile ayrışıyor).
  - **Testler:** F7 suite'leri gerçek `bagdam_dev` DB'sinde 2027 takvimiyle koşar ve kendi verisini siler; jest ile e2e F7 aynı anda koşmamalı (e2e'nin 2026 cycle'ları jest'in ileri `now`'ıyla kilitlenir). e2e F7 `commerce.dunning`/`paymentLinkHours` ayarlarını koşu süresince değiştirir ve geri alır.
  - Migration klasör adları Prisma zaman damgalı (`20260820183243_commerce` / `20260820183416_raw_commerce`; plandaki 0003/0004 mantıksal adlar).
- **F6 notları (2026-08-20):**
  - **Bağımlılık:** `nodemailer` bagdam workspace'inde kurulu değil (görev kuralı gereği `pnpm add` yapılmadı; SmtpTransport çalışma anında `require` eder) → gerçek SMTP gönderimi için `pnpm --filter @bagdam/api add nodemailer` (+ isteğe bağlı `@types/nodemailer`); kuruluncaya kadar `DISABLE_MAIL=false` ortamda MailLog FAILED `MAIL_TRANSPORT_UNAVAILABLE` (açık mesaj), `DISABLE_MAIL=true` (lokal/staging) etkilenmez. **DKIM imzalı gerçek gönderim (F6 DoD) lokalde doğrulanamadı** → F10b/staging'de SPF/DKIM/DMARC + SMTP ayarlarıyla; prod `.env WEB_URL` mutlak olmalı (verify/reset bağlantıları `${WEB_URL}/…`).
  - Handlebars ayrı paket değil: şablon motoru `hbs.create().handlebars` (hbs'in bağımlılığı handlebars@4.7.9, izole örnek) — hbs kaldırılırsa MailTemplateRenderer de etkilenir.
  - `MailLog @@unique([templateSlug, entityId])`: entityId verilen gönderimler (customer.* → userId, wholesale-lead → leadId) satırı UPSERT eder → aynı kullanıcıya ikinci reset maili önceki satırı ezer (son durum + createdAt tazelenir); tam gönderim geçmişi gerekirse entityId'ye damga eklenir (şema değişikliği yok).
  - Toptan yönetici bildirimi (`wholesale.new-lead` → `mail.wholesale-lead`) alıcı olarak `Setting site.contactEmail` (yoksa `.env SMTP_FROM`) ister; ikisi de yoksa sessizce atlanır + log (lokal e2e'de "yok") → panelden `site.contactEmail` girilmeli.
  - F5 açık notu kapandı: `toptan.hbs` formu artık `BahcedenCart.api()` ile gönderir (oturumlu müşteride X-CSRF-Token; cart.js yoksa düz fetch yedeği) — e2e F6 h2 adımı oturumlu 201.
  - Hız sınırları IP başına (register 5/dk · forgot 3/dk · login 10/dk · reset 5/dk · verify 30/dk): e2e art arda koşulursa 429 (forgot "bilinmeyen e-posta" kontrolü 429'u not düşer; koşular arası ≥1 dk önerilir).
  - cart.js adres PUT'u `zip` alanını her zaman gönderir (boş → sunucu null) → posta kodu silinebilir; `PUT /me/address` gövdesi `{fullName, phone, line, zoneSlug|zoneId, zip}`.
  - Admin müşteri detayı adres "İlçe / Bölge" alanı `zoneSlug` gösterir (MeAddress'te `zoneName` yok; `me.http.spec` anahtar kümesi sabit) — bölge adı istenirse ayrı alan F7+. Admin `lib/apiTypes.ts` F6 tipleri (AdminCustomer*/AdminMailLog*/AdminMailSendResult) shared ile aynı şekil ama yerel kopya (F5 notuyla aynı); `MailTestResult.previewPath` panelde tanınır.
  - Jest F6 harness'i (`__tests__/auth/f6-harness.ts`) MailLog satırlarını `logs/mail/<id>.html` önizlemeleriyle birlikte siler (`deleteMailLogsWithPreviews`) — testler yetim .html bırakmaz.
  - E-posta doğrulama zorunlu değil (giriş/sipariş engellemez); "doğrulama e-postasını yeniden gönder" ucu yok (P2). `DELETE /auth/me` (KVKK talebi) şema-var/UI-yok — anonimleştirme yalnız admin'den (ekran 16).
- **F5 notları (2026-08-20):**
  - `CatalogService.buildBootstrap` `deliveryDates`'i hâlâ kendi repository sorgusuyla kurar; `DeliveryService.getDates('urla',4)` ile eşdeğerliği delivery testi doğrular (aynı mapper kuralı). F7 cron `delivery-dates:generate` → `DeliveryService.generateDates()`; catalog'un DeliveryService'e bağlanması F7'de (döngü yok: Delivery → Settings, Catalog bağımsız).
  - Site `toptan.html` formu anonim çalışır; oturumlu müşteri (F6) için `BahcedenCart.api()` X-CSRF-Token ekleyecek (CsrfGuard access çerezi görünce başlık ister — e2e'de site formu ayrı anonim context'te çalıştırıldı; admin çerezleri aynı host/farklı portta da gönderilir).
  - Ayarlar: admin formu grubun TÜM alanlarını gönderir → zorunlu olmayan `number` boş/null gelirse alan **değişmez** (SKIP, settings.validation); sır alanları boş/maske → değişmez. "Sırrı temizle" ucu yok (e2e psql ile sildi) — F6 MailModule ile birlikte değerlendirilebilir.
  - Yasal: taslak sürüm silme ucu yok (admin'in açtığı v2+ taslaklar kalır; yayınlanmazsa zararsız; e2e psql ile temizledi). [B16] sonucu: politikalar.html JS nav'sız makaleyi `#slug` ile gösteriyor → 9. sekme gerekmedi (ADR-0003 istisnası kullanılmadı).
  - Kategori sekmeleri/panel notları `CatalogService.listActiveCategories()` (60 s cache; kategori mutasyonu `invalidateBootstrapCache` ile düşürür). SiteContent/legal/posts cache'leri ContentAdminService yazımında düşer (TTL 5 dk yalnız emniyet).
  - Admin `lib/apiTypes.ts` F5 tipleri shared'daki (AdminSiteContentItem / AdminLegalGroup / AdminSettingGroup / DeliveryZone / WholesaleLeadList …) ile aynı şekil ama yerel kopya; `export type {…} from '@bagdam/shared'` ile tekilleştirilebilir.
  - İçerik düzenlendikten sonra parite koşusu için `SEED_OVERWRITE_CONTENT=true pnpm db:seed` (admin değişikliklerini seed değerlerine çeker).

- PM2 cluster modunda `interpreter` yok sayılabilir → sunucuda `pm2 show bagdam-api` ile Node sürümü doğrulanacak; gerekirse fork modu (ecosystem `BAGDAM_EXEC_MODE=fork`).
- `styles.css` / `cart.js` için nginx'te 1 yıl immutable cache → F3'te `?v=` sürüm parametresi gelene kadar bu iki dosyaya kısa cache.
- ~~Admin API sözleşmesi varsayımları (`/auth/csrf`, `/auth/login`, `/auth/me`, `/auth/logout`) F4'te teyit edilecek.~~ → F4'te doğrulandı (e2e 12/12).
- **F4 açık (bağımlılık):** `sharp` bagdam workspace'inde kurulu değil (yalnız UA projesinin pnpm store'unda) → `pnpm --filter @bagdam/api add sharp` gerekli; kuruluncaya kadar `POST /admin/media` 503 (açık mesaj), diğer medya uçları çalışır. e2e bu koşuda sharp'ı geçici `NODE_PATH` ile UA store'undan yükleyerek webp+thumb dalını doğruladı. `bcrypt` + `@types/bcrypt` kök devDependencies'ten hoist ile çözülüyor → apps/api dependencies'e eklenmeli (prod `--prod` kurulumda kırılır). `@types/multer` yok (gerekmiyor: yerel `UploadedFileLike`).
- **F4 açık (deploy):** admin Vite çıktısı artık `dist/app/*` (`/assets/*` API medya yolu için boşaltıldı) → `deploy/nginx/admin*.conf`: `location /app/` immutable + `location /assets/` → API public (alias/proxy) eklenmeli; `/uploads/` location'ı zaten var. Dev/preview'da `/assets` + `/uploads` proxy'si vite.config.ts'te.
- F4 notu: Category.panelNote yalnız admin API'de (urunler.html panel metinleri F3'te statik; F5 CMS ile `{{site.*}}`). Bootstrap/public DTO'larda görsel yolu site-göreli (`assets/...` aynen, yüklemeler `uploads/<klasör>/<ad>.webp`; `media.mapper#toSiteMediaPath` tek kural); admin DTO'larında `/assets/...` | `/uploads/...`.
- F4 notu: testler (auth/catalog-admin/media spec) ve e2e gerçek `bagdam_dev` DB'sine yazar ve kendi verisini temizler; yalnız `audit_logs` satırları birikir (append-only, tasarım gereği). `apps/api/.env`'deki `PORT` geçici koşularda `PORT=4033 node dist/main.js` ile ezilir; 4010 dev API'sine dokunulmadı.
- ~~dunning `retryHours [24,72]` teslimat gününü aşabilir → F7 spike'ında pencere daraltılacak (state-machines.md §14).~~ → F7: denemeler teslimat günü 08:00 ile sınırlandı (state-machines §14 #1 KARAR); lansman öncesi admin'den `[2,12]` (bkz. F7 notları).
- Off-site yedek için sunucuda rclone/age yok → kurulum `deploy/README.md` §10.
- Raw SQL migration'larda kolon adları camelCase (`"userId"`, `"isDefault"`); F7 `raw_commerce` için `orders_orderNo_seq` / `"providerPaymentId"` kullanılacak (BACKEND-PLANI §2 notu güncellendi).
- `pnpm db:migrate -- --name x` yazımı prisma'yı interaktif soruya düşürür; doğru: `pnpm db:migrate --create-only --name <ad>` (CLAUDE.md'de).
- Kargo KDV oranı planda yok (vatTotal yalnız satır KDV'si) → fatura için F8 öncesi karar.
- `MediaFile.path` unique değil; F4 media:import aynı path-upsert kuralını kullanmalı (ileride additive `@@unique([path])`).
- Prisma 7 `package.json#prisma.seed` alanını kaldıracak → ileride `prisma.config.ts` (kapsam dışı).
